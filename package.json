{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./public/output.css --minify",
    "build:articles": "python3 build.py",
    "build": "npm run build:css && npm run build:articles",
    "dev:css": "npx @tailwindcss/cli -i ./src/input.css -o ./public/output.css --watch",
    "dev:articles": "chokidar \"src/**/*.md\" \"src/*.html\" -c \"python3 build.py\"",
    "dev": "npm-run-all --parallel dev:css dev:articles"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "tailwindcss": "^4.1.13"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "npm-run-all": "^4.1.5"
  }
}
